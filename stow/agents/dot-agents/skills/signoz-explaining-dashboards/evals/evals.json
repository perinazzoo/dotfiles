{
  "skill_name": "signoz-explaining-dashboards",
  "evals": [
    {
      "id": 1,
      "name": "redis-full-explain",
      "prompt": "Can you walk me through my 'Redis - Overview' dashboard? Want to understand what each panel shows and what to keep an eye on.",
      "expected_output": "Structured explanation of the Redis dashboard: overview, the host variable, panels grouped by the Grid sections in spec.layouts (About Redis, Overview, Performance Metrics, Memory Metrics, Base Activity Metrics, Logs), what to watch for per panel, and any health observations. Should notice that several composite members are disabled and that some panels reuse the same hit-rate formula.",
      "files": [],
      "assertions": [
        {"id": "a1", "text": "Output includes a high-level overview describing the dashboard's purpose (monitoring Redis)"},
        {"id": "a2", "text": "Output names the 'host' variable AND identifies it as a signoz/DynamicVariable"},
        {"id": "a3", "text": "Output groups panels using the dashboard's Grid section titles (About Redis, Overview, Performance Metrics, Memory Metrics, Base Activity Metrics, Logs)"},
        {"id": "a4", "text": "Output explains the cache hit-rate formula (A / (A + B)) * 100 in plain language"},
        {"id": "a5", "text": "Output gives 'what to watch for' guidance specific to Redis (mentions at least one of: evictions, fragmentation ratio, replication lag, blocked clients, slowlog)"},
        {"id": "a6", "text": "Output flags at least one structural issue: disabled base queries on multiple panels, duplicate Cache hit rate panels, or 'Hit rate'/'Cache hit rate' shown as a signoz/NumberPanel instead of a timeseries"},
        {"id": "a7", "text": "Agent resolves the dashboard name through paginated signoz_list_dashboards and calls signoz_get_dashboard with the returned id, never the name"},
        {"id": "a8", "text": "Output does NOT dump raw dashboard JSON or large query objects"},
        {"id": "a9", "text": "Output ends with concrete next-step offers (e.g., run queries, add panels/thresholds, modify dashboard)"}
      ]
    },
    {
      "id": 2,
      "name": "jvm-beginner-walkthrough",
      "prompt": "I'm new to JVM monitoring — help me understand my 'JVM Metrics Dashboard'. What do these metrics actually mean and which panels matter most for day-to-day vs. debugging?",
      "expected_output": "Beginner-friendly explanation: explains what JVM heap, GC, threads, and class loading actually mean in plain language, walks through the number/value panels (heap used, threads, CPU, classes), then the timeseries and the memory-pool table, and clearly distinguishes daily-watch panels from debugging-only panels. Should describe the service_name TextVariable as free-form input.",
      "files": [],
      "assertions": [
        {"id": "a1", "text": "Output explains in beginner-friendly terms what JVM heap memory means"},
        {"id": "a2", "text": "Output explains in beginner-friendly terms what garbage collection (GC) means"},
        {"id": "a3", "text": "Output names the 'service_name' variable AND identifies it as a TextVariable"},
        {"id": "a4", "text": "Output explicitly distinguishes which panels matter for daily watch vs debugging-only (the prompt asked for this)"},
        {"id": "a5", "text": "Output walks through the four signoz/NumberPanel value panels (heap used, threads, CPU, classes)"},
        {"id": "a6", "text": "Output mentions the Memory Pool Details table groups by jvm_memory_type and jvm_memory_pool_name"},
        {"id": "a7", "text": "Output mentions that GC Duration uses a rate aggregation (not raw counter)"},
        {"id": "a8", "text": "Agent resolves the dashboard name through paginated signoz_list_dashboards and calls signoz_get_dashboard with the returned id, never the name"},
        {"id": "a9", "text": "Output does NOT dump raw dashboard JSON or large query objects"}
      ]
    },
    {
      "id": 3,
      "name": "otel-collector-watch-for",
      "prompt": "explain the OpenTelemetry Collector Metrics Dashboard to me — specifically I want to know what I should be watching for and whether there are any obvious gaps or problems with how it's set up",
      "expected_output": "Explanation focused on what to watch for and structural observations of the large 65-panel dashboard. Should group panels by the Grid sections in spec.layouts, surface key health indicators (e.g., dropped spans/metrics, queue size, exporter failures), and flag structural issues like counters without a rate aggregation, missing thresholds, unused variables, or panels missing from every Grid.",
      "files": [],
      "assertions": [
        {"id": "a1", "text": "Output groups panels by Grid section titles (the dashboard has 7 sections)"},
        {"id": "a2", "text": "Output highlights at least 3 critical things to watch (e.g., dropped spans/metrics, queue size, exporter failures, memory limiter, refused items, batch send failures)"},
        {"id": "a3", "text": "Output surfaces structural observations or gaps (e.g., counters without rate function, missing thresholds, unused variables, very wide step intervals, high-cardinality groupBy, or duplicate panels)"},
        {"id": "a4", "text": "Output mentions both variables on the dashboard"},
        {"id": "a5", "text": "Output does NOT dump raw dashboard JSON or large query objects"},
        {"id": "a6", "text": "Output is well-organized despite the 65 panels — does not enumerate every panel as a flat list with no grouping"},
        {"id": "a7", "text": "Agent resolves the dashboard name through paginated signoz_list_dashboards and calls signoz_get_dashboard with the returned id, never the name"},
        {"id": "a8", "text": "Output addresses both parts of the prompt: what to watch AND structural problems/gaps"}
      ]
    },
    {
      "id": 4,
      "name": "name-resolution-on-second-page",
      "prompt": "Explain my Payments API dashboard.",
      "expected_output": "Using the offline fixture, the agent pages to offset 50 after page one, resolves Payments API to its id on page two, calls signoz_get_dashboard with that id, and explains the fetched Failed Payment Spans panel.",
      "files": ["evals/files/name-on-second-page.json"],
      "assertions": [
        {"id": "a1", "text": "Agent raises offset to 50 when the first signoz_list_dashboards page has no match, deriving it from total rather than a pagination.nextOffset field"},
        {"id": "a2", "text": "Agent calls signoz_get_dashboard with id 22222222-2222-4222-8222-222222222222, never with the name Payments API"},
        {"id": "a3", "text": "Agent does not explain the dashboard from list metadata alone"},
        {"id": "a4", "text": "Output explains that Failed Payment Spans counts errored traces for service.name=payments"}
      ]
    },
    {
      "id": 5,
      "name": "custom-variable-and-trace-panel",
      "prompt": "Explain my Trace Operations dashboard, including its region control and panel type.",
      "expected_output": "Using the offline fixture, the agent identifies region as a ListVariable backed by a signoz/CustomVariable plugin with a fixed set of choices, and explains Recent Checkout Traces as a list panel of raw trace rows filtered to checkout and the selected region.",
      "files": ["evals/files/custom-variable-trace-panel.json"],
      "assertions": [
        {"id": "a1", "text": "Output identifies region as a signoz/CustomVariable rather than a dynamic, query, or text variable"},
        {"id": "a2", "text": "Output explains that the configured region choices are us-east-1 and eu-west-1 and notes multi-select/ALL behavior"},
        {"id": "a3", "text": "Output identifies Recent Checkout Traces as a signoz/ListPanel, not a graph or generic table"},
        {"id": "a4", "text": "Output explains that the panel filters service.name=checkout and follows the selected $region value"},
        {"id": "a5", "text": "Agent resolves Trace Operations through signoz_list_dashboards and calls signoz_get_dashboard with its id"}
      ]
    }
  ]
}
